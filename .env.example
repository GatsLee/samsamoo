# API 서버 주소. 개발자가 실제 서버 주소로 바꾸면 목데이터 대신 실제 API를 탄다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# true 이면 services 계층이 mocks 데이터를 반환한다.
# 실제 API가 준비되면 false 로 바꾸기만 하면 된다.
NEXT_PUBLIC_USE_MOCK=true
